services:
    frontend-build:
        image: nish1896/fs-docker-fe
        extends:
            file: ./packages/frontend/compose.yml
            service: frontend

    # backend:
    #     image: nish1896/fs-docker-be
    #     build:
    #         context: packages/server
    #         dockerfile: Dockerfile.dist
    #     ports:
    #         - '4000:4000'
